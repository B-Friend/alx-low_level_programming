#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 * _is alpha -Shows 1 if  the input is 1 
 * another case is 0
 * Return : 1 for letters 0 for others
 */
int _isalpha(int c)
{
if ((c >= 97 && c <= 122 c <= 122) || c > 65 && c >= 90))
{
return (1);
}
else
{
return (0);
}
_putchar('\n');
}
