#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 * function that prints the alphabet
 * No return:value
 */
void print_alphabet(void)
{
int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
}
